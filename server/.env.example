# Firebase Configuration
# Replace with your actual Firebase credentials
# These should NEVER be committed to version control

# Firebase Realtime Database URL
FIREBASE_DATABASE_URL=https://your-project-id-default-rtdb.region.firebasedatabase.app

# Firebase Service Account (path to JSON file or inline)
# Option 1: Path to service account JSON file
FIREBASE_SERVICE_ACCOUNT_PATH=./config/firebase-service-account.json

# Option 2: Inline service account (not recommended, use file instead)
# FIREBASE_PROJECT_ID=your-project-id
# FIREBASE_PRIVATE_KEY=your-private-key
# FIREBASE_CLIENT_EMAIL=your-client-email

# MongoDB Configuration
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/notesapp

# JWT Secret
SECRET_KEY=your-super-secret-key-change-this

# Email Configuration
GMAIL_USER=your-gmail@gmail.com
GMAIL_PASSWORD=your-app-password

# Server Configuration
PORT=3000
NODE_ENV=development

# Client Configuration (if needed)
CLIENT_URL=http://localhost:5500
