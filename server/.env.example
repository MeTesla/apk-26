# Configuration de l'application

# Firebase Configuration
FIREBASE_DATABASE_URL=https://your-project-id-default-rtdb.region.firebasedatabase.app
FIREBASE_SERVICE_ACCOUNT_PATH=./config/firebase-service-account.json

# JWT Secret (générez une clé sécurisée)
SECRET_KEY=votre-cle-super-secrete-ici

# MongoDB Configuration
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/database_name

# Email Configuration (SendGrid)
# 1. Créez un compte sur https://sendgrid.com
# 2. Générez une API Key dans Settings > API Keys
# 3. Vérifiez votre domaine ou utilisez un expéditeur single sender
SENDGRID_API_KEY=SG.xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
EMAIL_FROM=noreply@votredomaine.com

# Server Configuration
PORT=3000
NODE_ENV=development

# Client Configuration
CLIENT_URL=http://localhost:5500/client

# CORS Origins (optionnel - valeurs par défaut définies)
# CORS_ORIGINS=http://127.0.0.1:5500,http://localhost:3000
